/*
 * RADIUM STYLES KEY:
 *
 * t = typography
 * p = positioning
 * w = whitespace
 *
 */

import t from './css/typography';
import p from './css/positioning';
import w from './css/whitespace';

export default {
  t, p, w,
};